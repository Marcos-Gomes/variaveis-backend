let raioCirc = 1;

const comprimentoCirc = 2 * Math.PI * raioCirc ** 2;

const areaCirc = Math.PI * raioCirc ** 2;

console.log(`O comprimento do círculo é: ${comprimentoCirc.toFixed(1)}`);
console.log(`A área do círculo é: ${areaCirc.toFixed(2)}`);